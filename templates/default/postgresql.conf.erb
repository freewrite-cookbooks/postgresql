<% @config.to_hash.sort.each do |key, value| %>
<% next unless key && value %>
<%= key %> = <%=
	case value
	when String
		"'#{value}'"
	when TrueClass
		'on'
	when FalseClass
		'off'
	else
		value
	end
%>
<% end %>
